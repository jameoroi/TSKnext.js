/* Release 61: the popup must be writable.  `noopener,noreferrer` returns an
   inaccessible WindowProxy in Chromium, which was the reason for blank PDFs. */
(function(){
  'use strict'; let orders=[];
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const money=v=>`${Number(v||0).toLocaleString('th-TH',{maximumFractionDigits:2})} บาท`;
  function wrapList(){const base=window.tskAdminOrdersList;if(typeof base!=='function'||base.__r61)return;const wrapped=async function(){const out=await base.apply(this,arguments);orders=Array.isArray(out?.orders)?out.orders:[];return out;};wrapped.__r61=true;window.tskAdminOrdersList=wrapped;}
  function openPurchaseOrder(id){
    const o=orders.find(x=>String(x.id)===String(id)); if(!o){alert('ยังโหลดรายละเอียดออเดอร์ไม่ครบ กรุณารีเฟรชรายการออเดอร์ แล้วลองใหม่');return;}
    const rows=(o.items||[]).map((i,n)=>`<tr><td>${n+1}</td><td><b>${esc(i.name||'-')}</b><br><small>SKU: ${esc(i.sku||'-')}</small></td><td>${Number(i.qty||0)}</td><td>${money(i.price)}</td><td>${money(Number(i.price||0)*Number(i.qty||0))}</td></tr>`).join('')||'<tr><td colspan="5">ไม่มีรายการสินค้า</td></tr>';
    const supplier=[...new Set((o.items||[]).map(i=>i.supplier_name).filter(Boolean))].join(', ')||'THAISERKIT SUPPLY / Platform inventory';
    const no=`PO-${esc(o.order_no||o.id)}`;
    const html=`<!doctype html><html lang="th"><head><meta charset="utf-8"><title>${no}</title><style>body{font:14px Arial,'Noto Sans Thai',sans-serif;color:#173b30;margin:42px}header{display:flex;justify-content:space-between;border-bottom:3px solid #0b5e43;padding-bottom:18px}h1{margin:0;font-size:27px}h2{font-size:15px;margin:24px 0 7px}.muted,small{color:#658077}table{width:100%;border-collapse:collapse;margin-top:12px}th{background:#0b5e43;color:#fff;text-align:left}th,td{padding:10px;border:1px solid #d9e4df;font-size:13px}.box{display:grid;grid-template-columns:1fr 1fr;gap:18px}.card{border:1px solid #d9e4df;border-radius:8px;padding:13px}.total{margin-left:auto;width:300px}.note{margin-top:24px;padding:12px;background:#f2faf6;border-left:4px solid #0b8a60}button{background:#0b5e43;color:#fff;border:0;border-radius:6px;padding:10px 16px;font-weight:bold}@media print{body{margin:18px}.print{display:none}}</style></head><body><header><div><h1>ใบสั่งซื้อสินค้า / PURCHASE ORDER</h1><div class="muted">THAISERKIT SUPPLY · เอกสารจัดซื้อเพื่อดำเนินการจัดส่ง</div></div><b>${no}</b></header><div class="box"><section class="card"><h2>บริษัทผู้จัดส่ง / Supplier</h2><b>${esc(supplier)}</b><p class="muted">อ้างอิงคำสั่งซื้อ: ${esc(o.order_no||'-')}</p></section><section class="card"><h2>ปลายทางจัดส่ง</h2><b>${esc(o.name||'-')}</b><br>${esc(o.phone||'-')}<br>${esc(o.address||'-')} ${esc(o.province||'')} ${esc(o.zip||'')}</section></div><h2>รายการที่ต้องจัดส่ง</h2><table><thead><tr><th>#</th><th>สินค้า</th><th>จำนวน</th><th>ราคาขาย</th><th>รวม</th></tr></thead><tbody>${rows}</tbody></table><table class="total"><tr><td>ยอดสินค้า</td><td>${money(o.subtotal)}</td></tr><tr><td>ค่าจัดส่ง</td><td>${money(o.shipping)}</td></tr><tr><th>ยอดตามออร์เดอร์</th><th>${money(o.total)}</th></tr></table><div class="note">สถานะชำระเงิน: ยืนยันแล้ว · ตรวจสอบ SKU จำนวน และที่อยู่ก่อนจัดส่ง</div><p class="print"><button onclick="window.print()">พิมพ์ / บันทึกเป็น PDF</button></p></body></html>`;
    const popup=window.open('', 'tsk_purchase_order', 'popup=yes,width=920,height=760'); if(!popup){alert('เบราว์เซอร์บล็อกหน้าต่างเอกสาร กรุณาอนุญาต Pop-up แล้วลองใหม่');return;}
    try{popup.document.open();popup.document.write(html);popup.document.close();popup.focus();try{popup.opener=null;}catch(_){}}catch(_){try{popup.location.href='data:text/html;charset=utf-8,'+encodeURIComponent(html);}catch(__){alert('เปิดเอกสารไม่สำเร็จ กรุณาลองใหม่');}}
  }
  window.adminOrderPurchaseOrder=openPurchaseOrder; wrapList(); setTimeout(()=>{wrapList();if(window.TSK_SESSION?.admin&&typeof window.adminOrdersRefresh==='function')window.adminOrdersRefresh({silent:true});},250); setTimeout(wrapList,1000);
})();
