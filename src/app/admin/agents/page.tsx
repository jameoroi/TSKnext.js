import { AdminPageHeader } from '@/components/admin/page-header';import { AgentManager } from '@/components/admin/agent-manager';import { getLegacySession } from '@/server/auth/legacy-session';import { safeLegacy } from '@/server/safe-legacy';export default async function Page(){const [d,s]=await Promise.all([safeLegacy<any>('admin.agents.list',{}, {agents:[],applications:[],levels:[]}),getLegacySession()]);return <><AdminPageHeader title="ตัวแทนจำหน่าย" description="Applications · approved agents · tier ladder · commission overrides"/><AgentManager initial={d} csrf={String(s.csrf||'')}/></>}
