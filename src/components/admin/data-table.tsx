'use client';
import { useMemo, useState } from 'react';
export type AdminRow=Record<string,unknown>;
function render(v:unknown){if(v==null||v==='')return '—';if(typeof v==='boolean')return v?'ใช้งาน':'ปิด';if(typeof v==='object')return JSON.stringify(v);return String(v)}
export function AdminDataTable({rows,columns,title='ข้อมูล'}:{rows:AdminRow[];columns?:Array<{key:string;label?:string}>;title?:string}){const [q,setQ]=useState('');const cols=columns?.length?columns:Object.keys(rows[0]||{}).filter(k=>!['description','password_hash','images','variants'].includes(k)).slice(0,8).map(key=>({key,label:key}));const filtered=useMemo(()=>{const n=q.trim().toLowerCase();return n?rows.filter(r=>JSON.stringify(r).toLowerCase().includes(n)):rows},[q,rows]);return <section className="overflow-hidden rounded-2xl border bg-white shadow-sm"><div className="flex flex-wrap items-center justify-between gap-3 border-b p-4"><div><h2 className="font-bold">{title}</h2><p className="text-xs text-slate-500">{filtered.length.toLocaleString()} รายการ</p></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ค้นหาในตาราง…" className="h-10 rounded-xl border bg-slate-50 px-3 text-sm outline-none focus:border-emerald-700"/></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{cols.map(c=><th key={c.key} className="px-4 py-3 font-semibold">{c.label||c.key}</th>)}</tr></thead><tbody>{filtered.map((row,i)=><tr key={String(row.id??row.key??row.code??i)} className="border-t align-top hover:bg-slate-50/70">{cols.map(c=><td key={c.key} className="max-w-[360px] truncate px-4 py-3" title={render(row[c.key])}>{render(row[c.key])}</td>)}</tr>)}{!filtered.length&&<tr><td className="px-4 py-12 text-center text-slate-400" colSpan={Math.max(1,cols.length)}>ยังไม่มีข้อมูล</td></tr>}</tbody></table></div></section>}
