import Link from 'next/link';export function PageHero({title,subtitle,eyebrow='THAISERKIT SUPPLY',action}:{title:string;subtitle?:string;eyebrow?:string;action?:{label:string;href:string}}){return <section className="bg-emerald-950 px-4 py-14 text-white"><div className="mx-auto max-w-7xl"><p className="text-xs font-bold tracking-[.22em] text-emerald-300">{eyebrow}</p><h1 className="mt-2 text-3xl font-bold md:text-5xl">{title}</h1>{subtitle&&<p className="mt-4 max-w-3xl text-emerald-50/70">{subtitle}</p>}{action&&<Link href={action.href} className="mt-6 inline-flex rounded-xl bg-white px-4 py-2.5 text-sm font-bold text-emerald-950">{action.label}</Link>}</div></section>}
export function StaticSections({sections}:{sections:Array<{title:string;body:string|string[]}>}){return <div className="mx-auto max-w-4xl space-y-4 px-4 py-10">{sections.map((s,i)=><section key={`${s.title}-${i}`} className="rounded-2xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">{s.title}</h2>{Array.isArray(s.body)?<ul className="mt-3 list-disc space-y-2 pl-5 text-sm leading-7 text-slate-600">{s.body.map(x=><li key={x}>{x}</li>)}</ul>:<p className="mt-3 whitespace-pre-line text-sm leading-7 text-slate-600">{s.body}</p>}</section>)}</div>}
